#Pede o pirmeiro número.
n1 = float(input('Primeira nota do aluno: '))

#Pede o segundo número.
n2 = float(input('Segunda nota do aluno: '))

#Mostra a média das notas
#(n1+n2)/2 -> Calcula a média, soma os números e divide pela quantidade de números. (que nesse caso são dois)
#:.1f -> Mostra apenas uma casa após o ponto.
print(f'A média entre {n1} e {n2} é igual a {(n1+n2)/2:.1f}')
